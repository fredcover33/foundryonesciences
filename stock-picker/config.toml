[probe]
trades_file_size_guard_gb = 6.0      # GUARD: refuse to download a trades
                                     # file larger than this
trades_stream_sample_seconds = 60    # wall-clock cap on the streaming test
target_replay_sessions = 60

[paths]
download_dir = "flatfiles"
store_path   = "data/picker.sqlite"
