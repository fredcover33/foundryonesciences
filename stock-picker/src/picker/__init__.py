"""picker — deterministic day-trading scanner (Block 0 scaffold).

Block 0 builds nothing that trades. This package currently contains only the
configuration loader, a flat-file access module, and a storage module.
"""
